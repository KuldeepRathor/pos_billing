import 'package:flutter/material.dart';

class WebStorePage extends StatelessWidget {
  const WebStorePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Web Store Page'),
      ),
    );
  }
}
